[package]
name = "sophon-verifier"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
sophon-config = { path = "../sophon-config" }
sophon-core = { path = "../sophon-core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
